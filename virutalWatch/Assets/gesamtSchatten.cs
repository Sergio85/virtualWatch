using UnityEngine;
using System.Collections;

public class gesamtSchatten : MonoBehaviour {

	public RenderTexture schattenTexture;
	public Camera cam;

	// Use this for initialization
	void Start () {
		//DepthTextureMode.Depth;
	}
	
	// Update is called once per frame
	void Update () {
		//schattenTexture.depth = cam.depth;

	}
}
